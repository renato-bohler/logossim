import styled from 'styled-components';
import { CanvasWidget } from '@projectstorm/react-canvas-core';

const Diagram = styled(CanvasWidget)`
  height: 100%;
  width: 100%;
`;

export default Diagram;
